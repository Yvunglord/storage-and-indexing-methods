## Сравнение производительности запросов в PostgreSQL с использованием индексов и традиционных методов.
### Запуск:
```bash
docker-compose up -d
docker-compose ps
docker exec -it task4-postgis psql -U postgres -d geom_database
```
