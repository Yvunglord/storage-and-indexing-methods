version: '3.8'
services:
  postgis:
    image: postgis/postgis:15-3.4
    container_name: task7-statistics
    environment:
      POSTGRES_DB: test_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 10_QWERTy
    ports:
      - "5433:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pg_data: